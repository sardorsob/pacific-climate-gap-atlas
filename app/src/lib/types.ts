// Map view modes. "default" shows the active score ramp; "coverage" is the
// signature "Where the data goes quiet" view; "uncertainty" re-encodes by rank
// range. Outlook is a separate gated boolean handled alongside these.
export type ViewMode = "default" | "coverage" | "uncertainty";
